import { classNames } from "shared/lib/classNames/classNames"
import cls from "./QuestionPage.module.scss"

const QuestionPage = () => {
	return <div className={classNames(cls.QuestionPage, {}, [])}></div>
}

export default QuestionPage
